# 📤 部署指南（把软件放到公网，手机随时随地用）

你有 3 种方式，从简单到强大：

## 方式一：本机使用（最省事，0 成本）

1. 双击项目里的 **启动学习助手.bat**（第一次会等 1-3 分钟装依赖，之后每次 10 秒左右）。
2. 它会自动打开浏览器，直接开始用。
3. 以后每次学习都双击这个 bat 就行。

> 缺点：只有你这台电脑能用。

## 方式二：局域网共享（电脑+手机，免费）

1. 双击 **启动学习助手.bat** 保持运行（窗口不要关）。
2. 双击 **查看手机访问地址.bat**，记下显示的地址（形如 http://192.168.1.5:4173）。
3. 手机连**同一个 Wi-Fi**，浏览器打开该地址即可。

> 缺点：手机必须和电脑连同一个 Wi-Fi。想装成 APP：手机浏览器菜单 →「添加到主屏幕」。

## 方式三：部署到公网（推荐，手机 4G/5G 随时随地用）

以 **GitHub Pages** 为例（免费、国内访问不稳定时可换 Gitee Pages，见文末）：

### 第 1 步：把软件打包

双击运行 **启动学习助手.bat** 完成一次构建，或者手动执行：

```bash
cd D:\Projects\English_Learn
npm run build
```

构建完成后，软件的所有文件都在 **dist/** 文件夹里。

### 第 2 步：创建 GitHub 账号并上传

1. 打开 https://github.com 注册并登录（免费）。
2. 点右上角 **+ → New repository**，名字随意（如 degree-english），选 **Public**，点 **Create repository**。
3. 在新页面里点 **uploading an existing file**，把 **dist 文件夹里面的所有内容**（不是 dist 文件夹本身）拖进去上传，点 **Commit changes**。

### 第 3 步：开启 Pages

1. 仓库页面点 **Settings** → 左侧 **Pages**。
2. **Source** 选 **Deploy from a branch**，Branch 选 **main** 和 **/(root)**，点 **Save**。
3. 等 1-2 分钟，页面顶部会出现网址：**https://你的用户名.github.io/degree-english/**

### 第 4 步：使用

- 电脑、手机浏览器直接打开上面的网址即可，**无需连 Wi-Fi，4G/5G 都行**。
- 手机浏览器 → 菜单 →「添加到主屏幕」→ 就是 APP 了，离线也能背单词。

> ⚠️ 更新内容时：重新 `npm run build`，把新的 dist 内容重新上传覆盖即可。

## 国内备选：Gitee Pages

1. 打开 https://gitee.com 注册登录，新建仓库（公开）。
2. 把 dist 内容上传到仓库。
3. 仓库 → 服务 → Gitee Pages → 选择分支部署（可能需要实名认证，免费）。

## ⚠️ 数据说明（重要）

- 你的学习进度存在**浏览器的本地存储**里，**不随网址同步**。
- 在电脑上学了 10 天，换到手机上是新的（0 进度）。
- 想换设备继续：在旧设备「设置 → 数据备份 → 导出备份」，把下载的 json 文件发到新设备，新设备打开同网址后「设置 → 导入备份」。

## 常见问题

| 问题 | 解决 |
|---|---|
| bat 双击闪退 | 右键 bat → 以管理员身份运行；或先确认已安装 Node.js |
| 端口被占用 | 关闭其他占用 4173 端口的程序后重试 |
| 手机打不开局域网地址 | 确认手机和电脑同一 Wi-Fi；关闭电脑防火墙或允许 Node.js 通过 |
| GitHub 网址打不开 | 国内网络波动，稍等重试；或使用 Gitee Pages / 局域网方式 |
